# Optional: enables in-app YouTube video search for local tracks.
# Browser keys are public. Restrict this key to your HTTP referrers and YouTube Data API v3.
# Without this key, the app provides a YouTube search link and accepts a pasted video URL.
VITE_YOUTUBE_API_KEY=
